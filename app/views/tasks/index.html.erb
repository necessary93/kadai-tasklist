<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>詳細<%= link_to task.id, task %> : <%= task.content %> &gt; &gt; &gt; 状態 ：<%= task.Status %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの作成', new_task_path %>